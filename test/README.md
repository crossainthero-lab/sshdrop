Integration tests that require an SSH/SFTP server should live here.

The current automated test suite uses temporary files and does not read the
user's real SSH keys or connect to real remote devices.
